export default {
  async nuxtServerInit ({ commit, dispatch }, { redirect, store }) {
    // await dispatch('site/getInfo')
    // if (!store.getters['site/status']) {
    //   redirect('/maintenance')
    // }
  }
}
